rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Note backups are stored exclusively in the user's personal Google Drive account.
    // Firebase Firestore is not used for personal data storage.
    // Default deny all access to documents.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
